{
  "title": "Patent Search MCP",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "tool": {
      "title": "Tool Name",
      "type": "string",
      "description": "MCP tool to call",
      "enum": ["search_patents", "get_patent_details", "find_patent_citations", "patent_landscape_by_company"],
      "editor": "select",
      "prefill": "search_patents"
    },
    "params": {
      "title": "Parameters (JSON)",
      "type": "object",
      "description": "Tool parameters as JSON object",
      "editor": "json",
      "prefill": {"query": "machine learning", "max_results": 1}
    }
  }
}