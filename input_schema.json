{
  "title": "Academic Research MCP",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "tool": {
      "title": "Tool Name",
      "type": "string",
      "description": "MCP tool to call",
      "enum": ["search_papers", "get_paper_details", "find_citations", "find_grants", "institution_research_profile", "author_research_profile", "research_trends", "systematic_review"],
      "editor": "select"
    },
    "params": {
      "title": "Parameters (JSON)",
      "type": "object",
      "description": "Tool parameters as JSON object",
      "editor": "json"
    }
  }
}
